function PropsFunctional(props)
{
    return(
        <div>
            <h1>My name is {props.name} I am studying at {props.college} My age is {props.age}</h1>
        </div>
    )
}

export default PropsFunctional;